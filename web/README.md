# Web Player

This folder contains:
- a web interface for running the Chip-8 emulator from the browser.
- a wrapper around the chip-8 emulator. This is compiled to wasm and consumed by the web player.

## Example Usage

```fish
cd web
npm i
npm run dev
```
